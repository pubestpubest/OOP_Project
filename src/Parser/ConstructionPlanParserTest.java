package Parser;
import Exceptions.EvaluationError;
import Exceptions.SyntaxError;
import Interfaces.Parser;
import Interfaces.Player;
import Interfaces.Tokenizer;
import org.junit.jupiter.api.Test;
import Tokenizer.*;

import java.util.List;

import static org.junit.jupiter.api.Assertions.*;
class ConstructionPlanParserTest {
    /*Player player = new PlayerContext();
    @Test
    void parseTest1() throws SyntaxError, EvaluationError {
        Tokenizer tokenizer = new ConstructionPlanTokenizer("done");
        Parser parser = new ConstructionPlanParser();
        List<Token> tokens = tokenizer.getTokens();
        parser.parse(tokens,player);
    }*/
}